To get the disease gameserver running you need to install docker and run the following in this directory:

docker-compose up -d

You probably want to change the default passwords in docker-compose.yaml ;)